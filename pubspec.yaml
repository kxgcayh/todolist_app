name: checklist_app
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  dio: ^4.0.4
  logger: ^1.1.0
  equatable: ^2.0.3
  velocity_x: ^3.3.0
  flutter_hooks: ^0.18.1
  hooks_riverpod: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: ^2.1.5
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
